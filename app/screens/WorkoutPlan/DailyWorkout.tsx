import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const DailyWorkout = (props: Props) => {
  return (
    <View>
      <Text>DailyWorkout</Text>
    </View>
  );
};

export default DailyWorkout;

const styles = StyleSheet.create({});
